{
  "name": "nbn-project-hub",
  "packageManager": "yarn@3.6.1",
  "workspaces": [
    "packages/nbn-js-lib",
    "packages/nbn-react-components",
    "apps/*"
  ],
  "scripts": {
    "start:demo": "yarn workspace demo start",
    "build:demo": "yarn build:nbn-react-components && yarn workspace demo build",
    "build:nbn-react-components": "yarn workspace nbn-react-components build"
  },
  "devDependencies": {
    "vite-plugin-dts": "^3.3.1"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.0.3"
  }
}
